apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'SeethaKey'
            keyPassword 'inferno2015'
            storeFile file('/home/faizaan/Desktop/keystore.jks')
            storePassword 'inferno2015'
        }
    }
    compileSdkVersion 22
    buildToolsVersion "22.0.1"
    defaultConfig {
        applicationId "com.khf.kavithai.annaikusamarpanam"
        minSdkVersion 14
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
    }
    buildTypes {

        release {
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:22.2.0'
    compile 'com.android.support:cardview-v7:22.2.0'

}
